MOVIEID_DB:
- type: RelationalDB
- kind: MySQL
- dependencies: []
- schema:
  - fields:
    - movieid.movieid: string
    - movieid.title: string
  - unfolded_fields: []
  - constraints:
    - PRIMARY(MOVIEID_DB.movieid.movieid)

MOVIEINFO_DB:
- type: RelationalDB
- kind: MySQL
- dependencies: []
- schema:
  - fields:
    - movieinfo.movieid: string
    - movieinfo.title: string
    - movieinfo.casts: varchar
  - unfolded_fields: []
  - constraints:
    - REFERENCE(MOVIEINFO_DB.movieinfo.movieid --> MOVIEID_DB.movieid.movieid) [+]
    - REFERENCE(MOVIEINFO_DB.movieinfo.title --> MOVIEID_DB.movieid.title) [+]
    - PRIMARY(MOVIEINFO_DB.movieinfo.movieid)
