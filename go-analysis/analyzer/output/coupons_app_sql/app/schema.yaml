COUPONS_DB:
- type: RelationalDB
- kind: MySQL
- dependencies: []
- schema:
  - fields:
    - coupons.category: string
    - coupons.value: int
    - claimed_coupons.coupon_id: int
    - claimed_coupons.*: SQL Table
    - claimed_coupons.user_id: int
    - coupons.coupon_id: int
  - unfolded_fields: []
  - constraints:
    - PRIMARY(COUPONS_DB.coupons.coupon_id)
    - PRIMARY(COUPONS_DB.claimed_coupons.coupon_id, COUPONS_DB.claimed_coupons.user_id)

STUDENTS_DB:
- type: RelationalDB
- kind: MySQL
- dependencies: []
- schema:
  - fields:
    - students.name: string
    - students.student_id: <unknown type>
    - students.*: SQL Table
    - students.balance: int
    - students.claimed_coupons: int
  - unfolded_fields: []
  - constraints:
    - REFERENCE(STUDENTS_DB.students.student_id --> COUPONS_DB.claimed_coupons.user_id)
      [+]
    - CHECK(students.balance > 0)
    - CHECK(students.claimed_coupons < 10) 